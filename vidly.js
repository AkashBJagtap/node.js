const manipulationObject = require('./manipulation')
const emotions = [ "Happy", "Neutral", "Exiting", "Surprise" ]

//console.log(module)
console.log(manipulationObject.isPresentFunction(emotions, "Happy"));